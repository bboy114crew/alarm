<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BottomTextStyle" parent="android:Widget.TextView">
        <item name="android:textColor">@color/text_dark</item>
        <item name="android:textSize">@dimen/bottom_text_size</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">5dp</item>
        <item name="android:gravity">center</item>
    </style>
    <style name="BottomImageView">
        <item name="android:layout_width">@dimen/image_view_width</item>
        <item name="android:layout_height">@dimen/image_view_height</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

    <style name="FragmentTextViewStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/margin_element_fragment</item>
        <item name="android:layout_marginRight">@dimen/margin_element_fragment</item>
        <item name="android:layout_marginTop">@dimen/margin_element_fragment</item>
    </style>
</resources>
